# Инструкции для Codex

- Новые переменные и функции называем только русскими словами латиницей.
- В коде не используем `delay`, только расчёт на базе `millis()`.
- Статические строки лучше помещать в PROGMEM с помощью макроса `F()`.
- При необходимости добавляйте заметки по стабильности работы.
